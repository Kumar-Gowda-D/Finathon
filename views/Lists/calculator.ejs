<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SIP Calculator</title>
    <link rel="stylesheet" href="/css/calculator.css">
</head>
<body>
    <div class="container">
        <h1>SIP Calculator</h1>
        <form action="/home/calculate/Recalculate" method="POST">
            <div class="slider-container">
                <label for="monthlyInvestment">Monthly Investment (₹): <span id="investment-value">25000</span></label>
                <input type="range" id="monthlyInvestment" name="monthlyInvestment" min="1000" max="100000" value="25000" oninput="updateValue('investment-value', this.value)">
            </div>

            <div class="slider-container">
                <label for="interestRate">Expected Return Rate (p.a.) (%): <span id="rate-value">14.3</span></label>
                <input type="range" id="interestRate" name="interestRate" min="1" max="20" step="0.1" value="14.3" oninput="updateValue('rate-value', this.value)">
            </div>

            <div class="slider-container">
                <label for="timePeriod">Time Period (Years): <span id="time-value">16</span></label>
                <input type="range" id="timePeriod" name="timePeriod" min="1" max="30" value="16" oninput="updateValue('time-value', this.value)">
            </div>

            <button type="submit">Calculate</button>
        </form>

        <% if (result) { %>
        <div class="results">
            <h2>Results</h2>
            <p>Invested Amount: ₹<%= result.investedAmount %></p>
            <p>Estimated Returns: ₹<%= result.estReturns %></p>
            <p>Total Value: ₹<%= result.totalValue %></p>
        </div>
        <% } %>
    </div>

    <script>
        function updateValue(id, value) {
            document.getElementById(id).innerText = value;
        }
    </script>
</body>
</html>
