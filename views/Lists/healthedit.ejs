<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Listing</title>
    <link rel="stylesheet" href="/css/edit.css">
</head>
<body>
    <div class="container">
        <h2>Edit Your Health Expenditure</h2>
        <form method="post" action="/home/health/<%=listing._id%>?_method=PUT" class="form">
            <div class="form-group">
                <label for="dateInput">Date</label>
                <input name="Listing[date]" value="<%=listing.date%>" type="text" id="dateInput" required>
            </div>

            <div class="form-group">
                <label for="amountInput">Amount (₹)</label>
                <input name="Listing[amount]" value="<%=listing.amount%>" type="number" id="amountInput" required>
            </div>

            <div class="form-group">
                <label for="spentInput">Description</label>
                <textarea name="Listing[spent]" id="spentInput" required><%=listing.spent%></textarea>
            </div>

            <button type="submit" class="btn">Save Changes</button>
        </form>
    </div>
</body>
</html>
